# 문의 폼 → 구글 시트 + 메일 알림 설정

사이트 문의 폼에 입력된 내용이 **구글 시트에 자동으로 쌓이고, 동시에 메일로 알림**이 오도록 만드는 과정입니다.
한 번만 설정하면 됩니다. 10분이면 끝납니다.

---

## 1단계 · 구글 시트 만들기

1. https://sheets.google.com 접속
2. **빈 스프레드시트** 클릭
3. 왼쪽 위 제목을 **`전혜영 강의문의`** 로 변경

---

## 2단계 · 스크립트 붙여넣기

1. 상단 메뉴에서 **확장 프로그램 → Apps Script** 클릭
2. 새 탭이 열리면 기존 코드(`function myFunction() {}`)를 **전부 지우고**
   아래 코드를 **그대로 붙여넣기**

```javascript
const SHEET_NAME = '문의';
const NOTIFY_TO  = 'nanicca00@naver.com';   // 알림 받을 메일 주소

function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);

    // 스팸봇 차단용 숨김칸이 채워져 있으면 무시
    if (data.website) return json({ ok: true });

    const ss = SpreadsheetApp.getActiveSpreadsheet();
    let sh = ss.getSheetByName(SHEET_NAME);
    if (!sh) sh = ss.insertSheet(SHEET_NAME);
    if (sh.getLastRow() === 0) {
      sh.appendRow(['접수시각', '이름 / 소속', '연락처', '문의 내용', '유입 경로']);
      sh.setFrozenRows(1);
    }

    const now = Utilities.formatDate(new Date(), 'Asia/Seoul', 'yyyy-MM-dd HH:mm');
    sh.appendRow([now, data.name, data.contact, data.message, data.ref || '']);

    MailApp.sendEmail({
      to: NOTIFY_TO,
      subject: '[강의 문의] ' + data.name,
      body:
        '사이트에서 강의 문의가 접수되었습니다.\n\n' +
        '접수시각 : ' + now + '\n' +
        '이름/소속 : ' + data.name + '\n' +
        '연락처   : ' + data.contact + '\n\n' +
        '문의 내용\n' + data.message + '\n\n' +
        '─────────────\n' +
        '시트에서 보기 : ' + ss.getUrl()
    });

    return json({ ok: true });
  } catch (err) {
    return json({ ok: false, error: String(err) });
  }
}

function doGet() {
  return json({ ok: true, msg: 'form endpoint' });
}

function json(obj) {
  return ContentService
    .createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}
```

3. 위쪽 **저장(디스켓 아이콘)** 클릭

---

## 3단계 · 웹앱으로 배포

1. 오른쪽 위 **배포 → 새 배포** 클릭
2. 왼쪽 **톱니바퀴 → 웹 앱** 선택
3. 아래처럼 설정

   | 항목 | 값 |
   |---|---|
   | 설명 | `문의 접수` (아무거나) |
   | 실행 계정 | **나 (본인 계정)** |
   | 액세스 권한 | **모든 사용자** ← 꼭 이걸로 |

4. **배포** 클릭
5. **권한 승인** 창이 뜹니다
   - 계정 선택 → "이 앱은 Google에서 확인하지 않았습니다" 경고가 나오면
     **고급 → (안전하지 않음) 프로젝트로 이동** → **허용**
   - 본인이 만든 스크립트라 안전합니다. 구글이 처음 보는 스크립트에 늘 띄우는 경고입니다.
6. 배포 완료 후 나오는 **웹 앱 URL** 을 복사

```
https://script.google.com/macros/s/AKfy............/exec
```

---

## 4단계 · 주소 알려주기

복사한 `/exec` 로 끝나는 주소를 알려주시면 사이트에 연결하겠습니다.

---

## 설정 후 이렇게 동작합니다

1. 담당자가 사이트에서 폼 작성 → **강의 문의 보내기** 클릭
2. 화면에 **"문의가 접수되었습니다"** 표시
3. **구글 시트에 한 줄 추가** (접수시각·이름/소속·연락처·문의내용)
4. **메일로 알림 도착** — 제목 `[강의 문의] 홍길동`

카카오톡·이메일 버튼은 그대로 남겨둡니다. 담당자가 편한 방법을 고르면 됩니다.

---

## 나중에 바꾸고 싶을 때

- **알림 메일 주소 변경** → Apps Script 에서 `NOTIFY_TO` 값을 수정하고 **배포 → 배포 관리 → 수정 → 버전: 새 버전 → 배포**
- **스크립트를 고친 뒤에는 반드시 새 버전으로 다시 배포**해야 반영됩니다
